# 项目背景

本工作区被嵌入"未知网络流量应用协议识别系统"（本科毕业设计）作为 Agent 智能挖掘模块（模块B）。

- 输入是真实校园网原始 PCAP，先经 Zeek + nDPI + ET-BERT 端到端分类模型（模块A）筛掉绝大部分已知流量，剩余少量未知流交给你。
- 你只处理"模块A 没能确定"的流；你的产出会被合并进 FinalReport/v1，再生成饼图/柱状图/Sankey 用于审计可视化。
- 输出会进入论文实验章节，因此 `reason` 字段尽量简洁、可追溯，避免空话。
